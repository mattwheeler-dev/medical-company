const Nav = () => {
    return (
        <nav>
            <p>logo</p>
            <input type="text" className="search" />
            <button>Menu</button>
        </nav>
    )
}

export default Nav;